@model IEnumerable<notienendqver.Models.Visitum>

@{
    ViewData["Title"] = "Index";
}

<h1>Visitas</h1>
<hr />
<table class="table table-light">
    <thead class="table table-success">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaVisita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DespVisita)
            </th>
            @*
            <th>
                @Html.DisplayNameFor(model => model.FechaCreacion)
            </th>
            
            <th>
                @Html.DisplayNameFor(model => model.CodBeneficiarioNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodViviendaNavigation)
            </th>
            *@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaVisita)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DespVisita)
            </td>
            @*
            <td>
                @Html.DisplayFor(modelItem => item.FechaCreacion)
            </td>
            
            <td>
                @Html.DisplayFor(modelItem => item.CodBeneficiarioNavigation.CodBeneficiario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodViviendaNavigation.CodVivienda)
            </td>
            *@
            <td>
                    <a asp-action="Edit" class="fa-solid fa-pen-to-square" asp-route-id="@item.CodVisita"></a>
                <a asp-action="Details" class="fa-solid fa-circle-info" asp-route-id="@item.CodVisita"></a>
                <a asp-action="Delete" class="fa-solid fa-trash" asp-route-id="@item.CodVisita"></a>
            </td>
        </tr>
}
    </tbody>
</table>
<p>
    <a asp-action="Create" class="btn btn-success">Programar visita</a>
</p>
